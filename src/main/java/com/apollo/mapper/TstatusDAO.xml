<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apollo.task.dao.TstatusDAO">
	
	<select id="getTstatuslist" parameterType="String" resultType="com.apollo.vo.TstatusDTO">
		select * from tstatus where methodologyid = (select methodologyid from project where pid = #{pid}) order by ordering asc
	</select>
	
</mapper>