<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apollo.member.dao.MemberDAO">

    <select id="getProjectMemberlist" parameterType="String" resultType="com.apollo.vo.MemberDTO">
    select * from member where mid in (select mid from pmember where pid = #{pid})
    </select>

    <select id="getInviteMemberList" parameterType="com.apollo.vo.MidpidDTO" resultType="com.apollo.vo.MemberDTO">
    select * from member where apollokey = (select apollokey from member where mid = #{mid}) and mid not in (select mid from pmember where pid = #{pid})
    </select>
    
    <insert id="insertPmember" parameterType="com.apollo.vo.MidpidDTO">
    	insert into pmember values(#{mid}, #{pid})
    </insert>
    
</mapper>