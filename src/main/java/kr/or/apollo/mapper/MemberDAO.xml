<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.apollo.dao.MemberDAO">
 <update id="findpwd" parameterType="kr.or.apollo.vo.MemberDTO">
  	update member set pwd=#{pwd} where mid=#{mid}
 </update> 
 <select id="findpwdidcheck" resultType="String">
		select mname from member where mid=#{mid}
</select>
 
</mapper>