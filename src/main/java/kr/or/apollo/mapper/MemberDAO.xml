<?xml version="1.0" encoding="UTF-8"?>
<insert id="insertMember" parameterType="vo.Member">
		insert into member(mid, mname, pwd, rday, pnum, image, deptname, apollokey, position)
		values(seq_mid.nextval, #{mname}, #{pwd}, #{rday}, #{pnum}, #{image}, #{deptname}, #{apollokey}, #{position})
</insert>